/// Represents a bubble with position, velocity, and size
class Bubble {
  double x;
  double y;
  double vx;
  double vy;
  double radius;

  Bubble({
    required this.x,
    required this.y,
    required this.vx,
    required this.vy,
    required this.radius,
  });
}
